{ "version": "2.0.0", "tasks": [ { "label": "Build", "type": "shell", "command": "gprbuild.exe",
            "args": [
                "${workspaceFolder}\\scratch.gpr",
                "-f",
                "-cargs",
                "-g"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}